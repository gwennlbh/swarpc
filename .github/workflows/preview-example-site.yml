name: Deploy example site preview

on:
  pull_request:
    branches: [main]

concurrency: preview-${{ github.ref }}

jobs:
  make-package:
    uses: ./.github/workflows/preview-packages.yml
  deploy-preview:
    needs: make-package
    runs-on: ubuntu-latest
    environment:
      name: "#${{ github.event.number }}: Example site preview"
      url: ${{ steps.deploy-preview.outputs.preview-url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version-file: package.json
          cache: npm

      - name: Install and Build
        if: github.event.action != 'closed' # You might want to skip the build if the PR has been closed
        run: |
          npm ci
          npm run build
          cd example
          npm add https://pkg.pr.new/gwennlbh/swarpc@${{ github.event.pull_request.number }}
          npm install 
          BUILD_COMMIT=${{ github.sha }} BASE_PATH=/cigale/_pullrequests/pr-$PR_NUMBER npm run build
        env:
          PR_NUMBER: ${{ github.event.number }}

      - name: Deploy preview
        id: deploy-preview
        uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: ./example/build
          umbrella-dir: _pullrequests
          comment: false
